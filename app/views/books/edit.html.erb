<%= render "shared/page_header", title: "Editing #{@book.name}", crumbs: [["books", books_path], [@book.name, book_path(@book)], ["edit"]] %>

<div class="container-fluid">
  <div class="form-wide">
    <%= render 'form', book: @book, cancel_path: book_path(@book), cancel_label: 'Cancel' %>
  </div>
</div>