<%= render "shared/page_header", title: "Editing #{@note.title}", crumbs: [["notes", notes_path], [@note.title, note_path(@note)], ["edit"]] %>

<div class="container-fluid">
  <div class="form-wide">
    <%= render 'form', note: @note, cancel_path: note_path(@note), cancel_label: 'Cancel' %>
  </div>
</div>