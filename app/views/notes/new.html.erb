<%= render "shared/page_header", title: "New Note", crumbs: [["notes", notes_path], ["new"]] %>

<div class="container-fluid">
  <div class="form-wide">
    <%= render 'form', note: @note, cancel_path: notes_path, cancel_label: 'Cancel' %>
  </div>
</div>