<!-- Main Sidebar Container -->
<%# AdminLTE 4: .main-sidebar pasó a .app-sidebar, y el tema oscuro ya no se
    pide con .sidebar-dark-primary sino con data-bs-theme de Bootstrap 5. %>
<aside class="app-sidebar bg-body-secondary shadow" data-bs-theme="dark">
  <!-- Brand Logo -->
  <div class="sidebar-brand">
    <a href="#" class="brand-link text-center">
      <%= image_tag "logo.png", class:"brand-image rounded-circle shadow", alt:"RN Logo", style:"opacity: .8" %>
      <span class="brand-text fw-light">RubyNotes</span>
    </a>
  </div>

  <!-- Sidebar -->
  <div class="sidebar-wrapper">
    <%if user_signed_in?%>
      <!-- Sidebar user panel (optional) -->
      <%# .user-panel / .image / .info no existen en v4: se arma con utilidades
          de Bootstrap. %>
      <div class="d-flex align-items-center gap-2 mt-3 pb-3 mb-3 px-3">
        <%= image_tag "user2-160x160.jpg", class:"rounded-circle shadow-sm", alt:"User Image", width: 34 %>
        <a href="#" class="d-block text-decoration-none"><%= current_user.email %></a>
      </div>
    <%end%> <!-- /.if user_signed_in? -->

    <!-- Sidebar Menu -->
    <nav class="mt-2">
      <%# .nav-sidebar pasó a .sidebar-menu y data-widget="treeview" a
          data-lte-toggle="treeview". %>
      <ul class="nav sidebar-menu flex-column" data-lte-toggle="treeview" role="menu" data-accordion="false">
        <!-- Add icons to the links using the .nav-icon class
              with font-awesome or any other icon font library -->
        <%if user_signed_in?%>
          <li class="nav-item">
            <%= link_to(new_note_path, class:"nav-link bg-lime") do %>
              <i class="nav-icon fas fa-plus"></i>
              <p>
                New Note
              </p>
            <%end%>
          </li>
          <li class="nav-item">
            <%= link_to(notes_path, class:"nav-link") do %>
              <i class="nav-icon fas fa-file-alt"></i>
              <p>
                All Notes
                <span class="nav-badge badge bg-danger me-3"><%= current_user.notes.count %></span>
              </p>
            <%end%>
          </li>
          <li class="nav-item">
            <!--sacar link no hace nada-->
            <%= link_to(books_path, class:"nav-link") do %>
              <i class="nav-icon fas fa-book"></i>
              <p>
                Books
                <i class="nav-arrow fas fa-angle-left"></i>
              </p>
            <%end%>
            <ul class="nav nav-treeview">
              <li class="nav-item">
                <%= link_to(new_book_path, class:"nav-link bg-lime") do %>
                  <i class="nav-icon fas fa-book-medical"></i>
                  <p>
                    New Book
                  </p>
                <%end%>
              </li>
              <li class="nav-item">
                <%= link_to(books_path, class:"nav-link") do %>
                  <i class="nav-icon fas fa-book"></i>
                  <p>
                    All Books
                    <span class="nav-badge badge bg-danger me-3"><%= current_user.books.count %></span>
                  </p>
                <%end%>
              </li>
              <% current_user.books.each do |book| %>
                <li class="nav-item">
                  <%= link_to(book, class:"nav-link") do %>
                    <i class="fas fa-book-open nav-icon"></i>
                    <p><%= book.name %></p>
                  <%end%>
                </li>
              <%end%>
            </ul>
          </li>
        <%else%> <!-- /.if user_signed_in? -->
          <li class="nav-item px-3">
            <%# BS5 eliminó .btn-block: se usa w-100. %>
            <%= link_to(new_user_session_path, class:"btn w-100 bg-primary text-white") do %>
              <i class="nav-icon fas fa-sign-in-alt"></i> Login
            <%end%>
          </li>
          <li class="nav-item mt-2 px-3">
            <%= link_to(new_user_registration_path, class:"btn w-100 bg-lime") do %>
              <i class="nav-icon fas fa-user-plus"></i> Sign Up
            <%end%>
          </li>
        <%end%> <!-- /.if user_signed_in? -->
      </ul>
    </nav>
    <!-- /.sidebar-menu -->
  </div>
  <!-- /.sidebar-wrapper -->
</aside>
